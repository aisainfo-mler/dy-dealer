/*
 * Translated default messages for the jQuery validation plugin.
 * Locale: CN
 */
jQuery.extend(jQuery.validator.messages, {
        required: text_required_check,
		remote: text_remote_check,
		email: text_email_check,
		url: text_url_check,
		date: text_date_check,
		dateISO: text_dateISO_check,
		number: text_number_check,
		digits: text_digits_check,
		creditcard: text_creditcard_check,
		equalTo: text_equalTo_check,
		accept: text_accept_check,
		maxlength: jQuery.validator.format(text_maxlength_check),
		minlength: jQuery.validator.format(text_minlength_check),
		rangelength: jQuery.validator.format(text_rangelength_check),
		range: jQuery.validator.format(text_range_check),
		max: jQuery.validator.format(text_max_check),
		min: jQuery.validator.format(text_min_check),
		
		username: jQuery.validator.format(text_username_check),
		usercode: jQuery.validator.format(text_usercode_check),
		password: jQuery.validator.format(text_password_check)
		
});

